'use strict'

class Card {
  constructor (suit, rank, value) {
    this.suit = suit
    this.rank = rank
    this.value = value
  }

  /* createCard (suit, rank, value) {
    return { suit: suit, rank: rank, value: value }
  } */
}

module.exports = Card
